.main
  .inner_content
    = render :partial => "users/dashboard_nav"
  .inner_content
    
    %h2 Create an Event
    
    - form_for([current_user, @event], :html => {:multipart => true}) do |form|
      .errors
        = error_messages_for :event
      %dl
        %dt= form.label :title
        %dd= form.text_field :title
        %dt= form.label :description
        %dd= form.text_area :description
        %dt= form.label :start_date
        %dd= form.datetime_select :start_date
        %dt= form.label :end_date
        %dd= form.datetime_select :end_date
        %dt= form.label :tags
        %dd= form.text_field :tag_list 
        %small Comma separate tags.    
      %p= form.submit 'Create', :disable_with => "Creating"
    
.details
  .inner_content
    = render :partial => "users/info"
  .inner_content
    = render :partial => "shared/advertisment"